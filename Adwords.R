mydata <- read.table("Adwords.csv", header=TRUE, 
                     sep=",",row.names='id')

str(Adwords)

hist(Adwords...Sheet1$X1.11.)
# Take the log-transform of the amount, and plot
Adwords$V25 = log(new_data$amount)
hist(new_data$amount)